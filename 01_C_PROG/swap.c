#include<stdio.h>

int main()
{
    int x = 10,b =1, c=-1;
    int y = 12,temp;
    float x = 2.5;
    temp = x;
    x = y;
    y = temp;
    

    printf("The swapped value of x and y is :- %d, %d", x,(b/c)<x);
}