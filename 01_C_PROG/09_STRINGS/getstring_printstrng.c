#include<stdio.h>
#include<string.h>

int main()
{
    char address[70];
    printf("\nEnter the address :");
    gets(address);
    
    printf("\nEntered string is : ");
    puts(address);

}